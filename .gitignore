node_modules/
.DS_Store
*.log
dist/

# runtime state (SQLite + cached artwork)
apps/*/data/

# secrets — MA token etc. Never commit.
.env
.env.*
!.env.example
*.secret
.claude/launch.json

# Marker written by deploy/pi/update.sh: the commit last built on the appliance
.crate-built
